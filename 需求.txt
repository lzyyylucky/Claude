关于框架
1 - 定义每一个功能模块的输入，输出，对它进行封装
2 - 把不同的功能放在不同的文件夹里面，做功能模块的划分，所有功能都用接口调用。
3 - 把不同的功能模块封装成不同的DLL，这样方便反复调用
4 -不能只有一个线程，要有主线程和子线程，子线程用于执行耗时操作，避免阻塞主线程，保证UI的流畅性。
5 -用户权限自己完成的事情，每个部分都做相应的功能划分。每个地方的接口，定义，写一份关于每个接口的详细的文档，调用哪个文件的代码之类的，每一个模块，每一个函数是干什么的
